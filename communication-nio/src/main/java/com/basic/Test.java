package com.basic;

import java.io.*;
import java.nio.ByteBuffer;

/**
 * locate com.basic
 * Created by MasterTj on 2019/3/8.
 */
public class Test {
    public static void main(String[] args) throws FileNotFoundException {
        BufferedReader br1=new BufferedReader(new InputStreamReader(new FileInputStream("")));
        BufferedReader br2=new BufferedReader(new FileReader(new File("")));
        BufferedInputStream bfi1=new BufferedInputStream(new FileInputStream(""));
        DataInputStream dataInputStream=new DataInputStream(new BufferedInputStream(new FileInputStream("")));
        byte[] bytes = "".getBytes();
        ByteBuffer wrap = ByteBuffer.wrap(bytes);
    }
}
